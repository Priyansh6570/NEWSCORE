// Base theme-token defaults and named template presets that tenants can adopt.
// A ThemeTokens default set and per-template presets will live here — CLAUDE.md §7–§8.

export const NEWSCORE_TOKENS_VERSION = '0.0.0';
