// Design system: presentational components, page blocks, and the block registry
// that maps a BlockType to its component + props schema — see CLAUDE.md §8.

export const NEWSCORE_UI_VERSION = '0.0.0';
