// Shared TypeScript types & DTOs used by both the API and the web/admin apps.
// SiteConfig, Block/Template types, the permission catalog, and API envelopes
// will live here as they are built — see CLAUDE.md §7–§10.

export const NEWSCORE_SHARED_VERSION = '0.0.0';
